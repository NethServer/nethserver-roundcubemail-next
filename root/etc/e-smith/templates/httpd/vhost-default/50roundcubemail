#
# 50roundcubemail
#
{
        if ( $Port == 80 || ! defined $SslMode )
           {
              $OUT .= " RewriteEngine On\n";
              $OUT .= " RewriteRule ^/roundcubemail(/.*)?\$ https://%{HTTP_HOST}/roundcubemail\$1 [R=301,L]\n";
              $OUT .= " RewriteRule ^/webmail(/.*)?\$ https://%{HTTP_HOST}/webmail\$1 [R=301,L]\n";

           }
}
